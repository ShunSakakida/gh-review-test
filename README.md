# gh-review-test

Sandbox repository for exercising the `gh-pr` review automation end-to-end
(live smoke of the real `gh` plumbing). Fixture PRs here are intentionally small
and single-purpose.

## Usage

```python
from src.greeting import greet
print(greet("world"))
```
